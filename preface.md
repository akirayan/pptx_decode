# はじめに

## なぜこのライブラリを作ったのか

PowerPointを使うたびに、同じことで苦労してきた。

フォントサイズの調整、シェイプの位置合わせ、色の設定。GUIの操作は直感的に見えて、実際には細かい数値を思い通りに制御できない。ピクセル単位の微調整、オブジェクトの整列、テキストボックスのサイズ。操作するたびに「もっと楽にできないか」と感じていた。

転機はAIとの対話だった。AIがPPTXを生成するPythonスクリプトを提示したとき、「これは使える」と直感した。スクリプトを自分で書けば、座標も色も数値で完全に制御できる。GUIのもどかしさから解放される。

ただし、まずPPTXの内部構造を理解しなければならない。どんなシェイプがあるか、どこに何があるか、フォントはどのように定義されているか。そこで最初に作ったのが `pptx_decode.py` だ。既存のPPTXファイルを読み込んで、構造を人間が読める形で出力する。PowerPointのGUIでは見えない内部情報が、コマンド一行で確認できるようになった。

構造が理解できたら次は生成だ。最初は `pptx_build.py` という形でPythonスクリプトにスライドの内容を直接書いた。しかし、資料を更新するたびにPythonコードを修正するのは結局手間がかかる。コードと内容が混在しているからだ。

そこで発想を変えた。**スライドの内容はYAMLで書く。生成はCLIで行う。**`pptx2yaml.py` でPPTXの内容をYAMLに書き出し、`yaml2pptx.py` でYAMLからPPTXを生成する。資料の内容を更新したければYAMLを編集するだけでよい。Pythonコードに触れる必要はない。

## 最大の難関：PlaceholderとBlank Slide

コーディングで最も理解に苦しんだのは、スライドマスターのレイアウトに埋め込まれた **Placeholder** というシェイプだった。

PowerPointのテンプレートには「スライドマスター」という概念がある。その下に複数の「レイアウト」があり、それぞれのレイアウトにはタイトル・本文・フッターなどの「プレースホルダー」が配置されている。プレースホルダーはただのシェイプではなく、レイアウトから継承され、スライドごとに異なる扱いを受ける特殊な存在だ。

この仕組みの理解に時間がかかった。プレースホルダーを通じてテキストを設定しようとすると、レイアウトとの継承関係が複雑で思い通りに動かない場面が何度もあった。

解決策は「逃げること」だった。

プレースホルダーと格闘するのをやめて、**Blank Slideを作り、全シェイプを削除する**。残るのは何もない白紙のスライドだ。そこに `add_rect()`、`add_text()`、`add_line()` で自由に描く。Placeholderの制約を受けず、座標と色と文字を完全に自分でコントロールできる。

テンプレートのロゴや底バーなどのブランドデザインはスライドマスターレイヤーに存在するため、全シェイプを削除しても継承される。Blank Slideはデザインを引き継ぎながら、コンテンツだけを自分で自由に配置できるキャンバスになる。

## 三十数年前の記憶

このアプローチには、三十数年前の経験との共鳴がある。

SunOSの開発環境でXViewのプログラミングをしていた頃、UIのPanelコンポーネントに不満を感じることがあった。ウィジェットの配置や外観をシステムに任せるのではなく、Xlibの描画関数を直接呼び出して、Panel上に自分の思い通りの表示を作る。座標を指定し、線を引き、テキストを配置する。それは手間のかかる方法だったが、完全な自由があった。

Blank Slideをキャンバスとして使い、座標で描画するこのライブラリのアプローチは、あの時の発想と同じだ。フレームワークの「良い使い方」から外れているかもしれないが、自分が何をしているかが明確で、制御が完全に自分の手にある。

PowerPointというGUIツールをPythonスクリプトから操作するこの試みも、結局は「システムの上で自分の思い通りに描く」という同じ動機から来ている。
